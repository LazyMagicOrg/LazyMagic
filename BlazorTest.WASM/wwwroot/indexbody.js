// Use addLink() and addHeadScript() to dynamically load styles and scripts
console.log("Adding body links and scripts dynamically...");

// Add Bootstrap JavaScript bundle (required for tabs, accordions, modals, etc.)
addBodyScript('https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js');